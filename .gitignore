datasets/
node_modules/
dist/
!installer/dist/
**/_tree-sitter/
*.log
.DS_Store
.env
.env.local
*.tmp
*.temp
.install-version
.claude/settings.local.json
.claude/agents/
.claude/skills/
.claude/plans/
.claude/worktrees/
plugin/data/
plugin/data.backup/
package-lock.json
bun.lock
private/
datasets/
Auto Run Docs/

# Generated UI files (built from viewer-template.html)
src/ui/viewer.html

# Local MCP server config (for development only)
.mcp.json
.cursor/

# Prevent literal tilde directories (path validation bug artifacts)
~*/

# Prevent other malformed path directories
http*/
https*/

# Built viewer bundle (regenerated by build)
plugin/ui/viewer-bundle.js

# Local machine config (contains tailnet/IP topology -- copy machines.example.ts to get started)
src/ui/viewer/utils/machines.ts

# Screenshots (may contain machine-specific info)
docs/screenshots/

# Ignore WebStorm project files (for dinosaur IDE users)
.idea/

# Playwright trace/log artifacts
.playwright-cli/

# Project-local development instructions (not tracked)
CLAUDE.md
AGENTS.md
